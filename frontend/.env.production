# NGS&O CRM Gestión - Production Environment Variables

# API Backend (usar ruta relativa para que nginx proxy funcione)
VITE_API_URL=/api/v1

# WebSocket (vacío para usar URL relativa, nginx proxy se encarga)
VITE_SOCKET_URL=
